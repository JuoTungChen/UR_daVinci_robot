cmake_minimum_required(VERSION 2.8.3)
project(ursurg_description)

find_package(catkin REQUIRED)

catkin_package()

# set(xacro_files
#   urdf/lnd.urdf.xacro
# )

# xacro_add_files(${xacro_files}
#   INORDER
#   TARGET xacro_target
#   INSTALL DESTINATION urdf
# )
